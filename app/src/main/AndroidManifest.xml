<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".core.Base"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".presentaion.dymaic.DyamicActivity"
            android:exported="false" />
        <activity
            android:name=".presentaion.page.StaticActivity"
            android:exported="false" />
        <activity
            android:name=".presentaion.HtmlPager.ui.HtmlPagerActivity"
            android:exported="false" />
        <activity
            android:name=".presentaion.flippage.ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentaion.book.BookActivity"
            android:exported="false" />
        <activity
            android:name=".core.SwitchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>